const names = ["alice", "bob", "charlie"];

const upperName = names.map(function (name) {
  return name.toUpperCase();
});

console.log(upperName);

// const upperName = names.map((name) => name.toUpperCase());

console.log(upperName);
